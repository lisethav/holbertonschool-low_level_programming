#include "holberton.h"

/**
 * _isupper - is C o c
 *
 * @c: its variable
 * Return: the end
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
